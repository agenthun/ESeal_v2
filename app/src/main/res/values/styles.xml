<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimaryDark">@color/cube_holo_blue_dark</item>
        <item name="colorPrimary">@color/cube_mints_main_color</item>
        <item name="android:windowNoTitle">true</item>
        <item name="colorAccent">@color/cube_mints_white</item>
        <item name="android:textColorHint">@color/highlighted_text_material_dark</item>

    </style>

</resources>
